

const Input = ({className, placeholder, type, name}) => {
  return (
    <Input
      className={className}
      placeholder={placeholder}
      type={type}
      name={name}
    />
  );
}

export default Input